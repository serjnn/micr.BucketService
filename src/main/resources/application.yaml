spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/micr-synch
    username: qwe
    password: qwe

  application:
    name: Bucket

server:
  port: 7001
